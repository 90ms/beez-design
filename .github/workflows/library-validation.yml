name: Validate BEEZ Libraries

on:
  push:
    branches:
      - main
    paths:
      - "settings.gradle.kts"
      - "build.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradle/libs.versions.toml"
      - "beez-*/**"
      - "specification/**"
      - "scripts/**"
      - ".github/workflows/library-validation.yml"
  pull_request:
    paths:
      - "settings.gradle.kts"
      - "build.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradle/libs.versions.toml"
      - "beez-*/**"
      - "specification/**"
      - "scripts/**"
      - ".github/workflows/library-validation.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: library-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Compile and test libraries
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle 9.3.1
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: "9.3.1"
          cache-provider: basic

      - name: Validate token sources
        run: node scripts/validate-tokens.mjs

      - name: Show toolchain
        run: gradle --version

      - name: Compile and test
        run: gradle --no-daemon --stacktrace build

      - name: Upload Gradle reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: beez-gradle-reports
          path: |
            **/build/reports/
            **/build/test-results/
          if-no-files-found: ignore
