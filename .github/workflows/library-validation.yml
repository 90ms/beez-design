name: Validate BEEZ Libraries

on:
  push:
    branches:
      - main
    paths:
      - "settings.gradle.kts"
      - "build.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradle/libs.versions.toml"
      - "beez-*/**"
      - "samples/published-consumer/**"
      - "specification/**"
      - "scripts/**"
      - ".github/workflows/library-validation.yml"
  pull_request:
    paths:
      - "settings.gradle.kts"
      - "build.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradle/libs.versions.toml"
      - "beez-*/**"
      - "samples/published-consumer/**"
      - "specification/**"
      - "scripts/**"
      - ".github/workflows/library-validation.yml"
  workflow_dispatch:
    inputs:
      beez_version:
        description: Version to use for staging publication validation
        required: true
        default: 0.1.0-alpha.1
        type: string

permissions:
  contents: read

concurrency:
  group: library-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Compile and test libraries
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      BEEZ_VERSION: ${{ inputs.beez_version || '0.1.0-SNAPSHOT' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle 9.3.1
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: "9.3.1"
          cache-provider: basic

      - name: Validate token sources
        run: node scripts/validate-tokens.mjs

      - name: Verify token generation
        run: |
          node --test scripts/token-tools.test.mjs scripts/token-kotlin.test.mjs
          node scripts/generate-tokens.mjs --check

      - name: Validate build version
        run: |
          node --test scripts/release-version.test.mjs
          node scripts/release-version.mjs "$BEEZ_VERSION"

      - name: Show toolchain
        run: gradle --version

      - name: Compile and test
        run: xvfb-run --auto-servernum --server-args="-screen 0 1280x1024x24" gradle --no-daemon --stacktrace build -PbeezVersion="$BEEZ_VERSION"

      - name: Publish libraries to staging repository
        run: gradle --no-daemon --stacktrace publishLibrariesToStagingRepository -PbeezVersion="$BEEZ_VERSION"

      - name: Verify published artifact consumer
        working-directory: samples/published-consumer
        run: gradle --no-daemon --stacktrace build -PbeezVersion="$BEEZ_VERSION"

      - name: Upload staging repository
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: beez-staging-${{ inputs.beez_version }}
          path: build/staging-repository/
          if-no-files-found: error
          retention-days: 7

      - name: Upload Gradle reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: beez-gradle-reports
          path: |
            **/build/reports/
            **/build/test-results/
          if-no-files-found: ignore
